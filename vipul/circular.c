#include<stdio.h>
#include<stdlib.h>
#include<sstring.h>

struct CBP{
    int id;
    struct CBP *ptr;
};

typedef struct CBP Node;

struct Node* last = NULL;

Node* creatNode(int);
void disp(Node*);


Node* creatNode(int data){
    Node *prt = malloc(sizeof(struct CBP));
    if (ptr !== NULL){
        ptr-> id = data;
        ptr-> ptr = NULL;
    }
    
void disp(){
    
}

int main(){
    
}
